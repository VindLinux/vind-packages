System-wide Clang configuration file that makes clang++ default to -stdlib=libc++, since this musl-based toolchain has no GCC/libstdc++ and every C++ build must otherwise pass -stdlib=libc++ explicitly.
